= form_for(quack) do |f|
  - if quack.errors.any?
    #error_explanation
      %h2
        = pluralize(quack.errors.count, "error")
        prohibited this quack from being saved:
      %ul
        - quack.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label :content
    = f.text_field :content
  .actions
    = f.submit
